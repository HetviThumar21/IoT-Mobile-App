import 'package:flutter/material.dart';

class OperatorAlerts extends StatelessWidget {
  const OperatorAlerts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Operator Alerts",
          style: TextStyle(color: Colors.white)),
    );
  }
}
